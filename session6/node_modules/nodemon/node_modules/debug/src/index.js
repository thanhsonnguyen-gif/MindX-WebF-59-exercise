"use strict";

/**
 * Detect Electron renderer / nwjs process, which is node, but we should
 * treat as a browser.
 */
if (typeof process === 'undefined' || process.type === 'renderer' || process.browser === true || process.__nwjs) {
  module.exports = require('nodemon/node_modules/debug/src/browser.js.js');
} else {
  module.exports = require('nodemon/node_modules/debug/src/node.js.js');
}

